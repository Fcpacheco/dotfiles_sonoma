- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config/bat:
      create: true
      path: config/bat


- create:
    - ~/work

- shell:
#   - [git submodule update --init --recursive, Installing submodules]
    
    -
      command:  ./setup_homebrew.zsh
      stdout: true
      stderr: true
